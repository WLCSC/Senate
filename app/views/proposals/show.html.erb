<%= render :partial => 'proposal', :locals => {:item => @proposal, :depth => nil, :comments => true} %>
<%= tagbox @proposal %>
	<%= nested_comments @proposal.comments, false, 3 %>
	<%= render :partial => 'comments/form', :locals => {:parent => @proposal} unless @proposal.closed %>
